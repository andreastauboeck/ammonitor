services:
  ammonitor:
    build: .
    ports:
      - "8888:8000"
    env_file:
      - ./.env
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-dev}
      - VERSION=${VERSION:-development}
    restart: unless-stopped